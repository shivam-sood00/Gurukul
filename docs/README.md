# Root Docs

The public documentation source is `website/docs/`.

Keep this directory for non-secret working research notes and audits. It is tracked in a public repository, so do not
store private maintainer context, credentials, private integration details, or sensitive investigation logs here. When
notes become maintained user-facing documentation, move them into `website/docs/` and add them to
`website/sidebars.ts`.
